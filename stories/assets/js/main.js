// require('domready')(function () {
    // document.querySelectorAll('#control-dataTheme').forEach((el) => {
    //     el.addEventListener('change', () => {
    //         alert('change');
    //         if (el.value === 'dark') {
    //             el.closest('.docs-story').setAttribute('data-theme', 'dark'); // for docs
    //             el.closest('#storybook-root').setAttribute('data-theme', 'dark'); // for stories
    //         } else {
    //             el.closest('.docs-story').setAttribute('data-theme', 'light'); // for docs
    //             el.closest('#storybook-root').setAttribute('data-theme', 'light'); // for stories
    //         }
    //     });
    // });
// });